<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.android.ocrmain.MainActivity"
    android:background="@color/black"
    >
    <View
        android:id="@+id/am_background_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/black"
        />
    <android.support.v4.view.ViewPager
        android:id="@+id/am_view_pager"
        android:layout_height="match_parent"
        android:layout_width="wrap_content"
        />
   <!-- <android.support.design.widget.TabLayout
        android:id="@+id/am_tab_layout"
        android:layout_marginTop="24dp"
        android:layout_width="match_parent"
        android:layout_height="@dimen/abc_action_bar_default_height_material"
        android:background="@color/black"
        app:tabTextColor="@color/white"
        app:tabSelectedTextColor="@color/white_t75"
        /-->

    <com.example.android.ocrmain.view.SnapTabViews
        android:id="@+id/am_snap_tabs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"

        android:layout_alignParentBottom="true"
        android:layout_marginBottom="0dp"
        android:background="@color/white"
        />

</RelativeLayout>